'use strict';

describe('myApp.FontPicker module', function() {

  beforeEach(module('myApp.FontPicker'));

  describe('FontPicker controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var FontPickerCtrl = $controller('FontPickerCtrl');
      expect(FontPickerCtrl).toBeDefined();
    }));

  });
});